<?php require('template/header.phtml') ?>

<h3><b>Basket</b></h3>


<?php if ( isset( $_SESSION['selectedCDs'] ) && !empty($_SESSION['selectedCDs'])):?>
	<h4><b>Your cds:</b> </h4>
	<table class="table-bordered">
		<thead>
		<tr>
			<th>CD details</th>
			<th>Price</th>
		</tr>
		</thead>
		<tbody>

		<?php foreach ($_SESSION['selectedCDs'] as $cd) : $cd=unserialize($cd);?>
			<tr>
				<td>
					<p><?php echo $cd->title;?> </p>
				</td>
				<td>&pound<?php echo $cd->price;?>
				</td>
			</tr>
		<?php endforeach; ?>
		</tbody>
	</table>

	<p><h3>Total rental cost = &pound<?php echo $totalCost ?></h3></p>
<form action="<?php echo $_SERVER['PHP_SELF']; ?>" method="post">
	<input type="submit" name="payAndLogout" value="Pay for DVD and Logout">
</form>

<?php else: ?>
<div class="text-info alert-warning">
	<h4><b>Your basket is empty.</b> </h4>
	You can add CDs from <a href="allCDs.php">here</a>
</div>
    <?php endif;?>
<?php require('template/footer.phtml') ?>
